workspace:
  url: https://code.stanford.edu
  project: bil/code/2603_scicd
  cicd:
    default:
      before_script:
        - python3 -m pip install -e .[test]
    workflow:
      rules:
        - if: '$$CI_PIPELINE_SOURCE == "push"'
task:
  image: "python:3.10"
  tags: ["bilkube"]
